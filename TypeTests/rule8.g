fun int main(){
    // Passes
    var int x = 1;
    var int y = 2;
    var int passes = x + y;
    var string s = "Hello";

    // Fails
    var int fails = "Hello" + "World";
    var string alsofails = "Hello" + "World";
    s = s + 1;

    //passes
    x = y + 3;

}
